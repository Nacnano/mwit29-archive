/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    appName: "MWIT29 Archive",
    // appUrl: 'https://mwit29-archive.vercel.app',
    appUrl: "http://localhost:3000",
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
